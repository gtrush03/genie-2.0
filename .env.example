# ──────────────────────────────────────────────────────────────────────────────
# GENIE — environment config
# Copy to .env and fill in your own values. Never commit the real .env.
# ──────────────────────────────────────────────────────────────────────────────

# === BRAIN ===
# Used by the legacy interpreter and the research-topic helper.
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENROUTER_MODEL=anthropic/claude-sonnet-4-6

# === DISPATCHER (Claurst as the execution engine) ===
# Optional overrides. Defaults are fine for most cases.
# GENIE_CLAURST_BIN=/path/to/claurst
# GENIE_CLAUDE_MODEL=sonnet          # or: opus
# GENIE_MAX_TURNS=50
# GENIE_MAX_BUDGET_USD=2
# GENIE_CLAUDE_TIMEOUT_MS=3600000    # 60 min safety net; set 0 to disable
ANTHROPIC_API_KEY=sk-ant-your-key-here

# === FIREHOSE (JellyJelly) ===
JELLY_API_URL=https://api.jellyjelly.com/v3
GENIE_KEYWORD=genie
GENIE_POLL_INTERVAL=3000             # main poll interval (ms)
GENIE_FAST_RETRY_INTERVAL=1500       # per-clip transcript-watch interval (ms)
GENIE_FAST_RETRY_MAX_MS=300000       # max per-clip wait for transcript (ms)
GENIE_WATCHED_USERS=                 # empty = watch all users

# === TELEGRAM (one-way reporting channel) ===
TELEGRAM_BOT_TOKEN=123456789:your-bot-token-here
TELEGRAM_CHAT_ID=123456789

# === DEPLOY (Vercel) ===
# Vercel CLI must be logged in on the host (`vercel login`).
GH_OWNER=your-github-username

# === STRIPE (payment links + invoices via CLI) ===
# Test mode keys are safe for development. Swap to sk_live_* for production.
STRIPE_SECRET_KEY=sk_test_your-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key
STRIPE_API_KEY=sk_test_your-secret-key

# === GEMINI (optional — vision / grounding) ===
GEMINI_API_KEY=your-gemini-key

# === JELLYJELLY COMMENTS ===
# JWT auth token for commenting on JellyJelly clips after fulfilling wishes.
# Get this from your JellyJelly account (Supabase auth token).
# JELLY_AUTH_TOKEN=eyJ...your-token-here

# === BROWSER (persistent Chrome via launchd) ===
GENIE_BROWSER_PROFILE=~/.genie/browser-profile
GENIE_CDP_ENDPOINT=http://127.0.0.1:9222
GENIE_HEADED=true

# === Optional integrations ===
# APOLLO_API_KEY=
# RESEND_API_KEY=
